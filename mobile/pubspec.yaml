name: memolink_mobile
description: MemoLink P2P memo sync — mobile client
publish_to: "none"
version: 1.0.5+5

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  sqflite: ^2.4.1
  path: ^1.9.0
  path_provider: ^2.1.5
  crypto: ^3.0.6
  uuid: ^4.5.1
  mobile_scanner: ^6.0.2
  shared_preferences: ^2.3.4
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
